plugins {
    id 'java'
    id 'com.github.johnrengelman.shadow' version '8.1.1'
}

group = 'com.ikingsnipe'
version = '16.0.0'

java {
    toolchain {
<<<<<<< Updated upstream
        languageVersion = JavaLanguageVersion.of(17)
    }
=======
        // Use JDK 17 (which you have installed)
        languageVersion = JavaLanguageVersion.of(17)
    }
    // DreamBot expects Java 8 bytecode
>>>>>>> Stashed changes
    sourceCompatibility = JavaVersion.VERSION_1_8
    targetCompatibility = JavaVersion.VERSION_1_8
}

repositories {
    mavenCentral()
}

dependencies {
    compileOnly files('libs/dreambot-api.jar')
}

shadowJar {
    archiveBaseName.set('snipes_scripts_enterprise')
    archiveClassifier.set('')
    archiveVersion.set('')

    manifest {
        attributes(
            'Implementation-Title': 'snipes♧scripts Enterprise',
            'Implementation-Version': project.version
        )
    }
}

tasks.withType(JavaCompile).configureEach {
    options.encoding = 'UTF-8'
    options.compilerArgs += ['-Xlint:deprecation', '-Xlint:unchecked', '-parameters']
}

tasks.build.dependsOn tasks.shadowJar
